# @narai/notion-agent-connector publishes only the compiled library.
# Everything else — source, tests, the Claude Code plugin, etc. — is
# excluded from the npm tarball.
src/
tests/
plugin/
vitest.config.ts
tsconfig.json
.gitignore
.git/
